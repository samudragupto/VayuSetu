name: Frontend - build and deploy

on:
  push:
    branches: [main]
    paths:
      - "frontend/**"
      - "firebase.json"
      - "firestore.rules"
      - "firestore.indexes.json"
      - "storage.rules"
      - ".github/workflows/frontend-deploy.yml"
      - "scripts/check_deploy_config.sh"
      - "scripts/deployment_preflight.sh"
  pull_request:
    branches: [main]
    paths:
      - "frontend/**"
      - "firebase.json"
      - "firestore.rules"
      - "firestore.indexes.json"
      - "storage.rules"
      - ".github/workflows/frontend-deploy.yml"
      - "scripts/check_deploy_config.sh"
      - "scripts/deployment_preflight.sh"
  workflow_dispatch:

permissions:
  contents: read
  id-token: write

concurrency:
  group: frontend-${{ github.ref }}
  cancel-in-progress: true

env:
  NODE_VERSION: "20"

jobs:
  build:
    name: Lint, type check and build
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    env:
      NEXT_PUBLIC_FIREBASE_API_KEY: ${{ vars.NEXT_PUBLIC_FIREBASE_API_KEY }}
      NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN: ${{ vars.NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN }}
      NEXT_PUBLIC_FIREBASE_PROJECT_ID: ${{ vars.GCP_PROJECT_ID }}
      NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET: ${{ vars.NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET }}
      NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID: ${{ vars.NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID }}
      NEXT_PUBLIC_FIREBASE_APP_ID: ${{ vars.NEXT_PUBLIC_FIREBASE_APP_ID }}
      NEXT_PUBLIC_GOOGLE_MAPS_API_KEY: ${{ vars.NEXT_PUBLIC_GOOGLE_MAPS_API_KEY }}
      NEXT_PUBLIC_GOOGLE_MAPS_MAP_ID: ${{ vars.NEXT_PUBLIC_GOOGLE_MAPS_MAP_ID }}
      NEXT_PUBLIC_ADMIN_DOMAIN: ${{ vars.ADMIN_DOMAIN }}
      NEXT_PUBLIC_API_GATEWAY_URL: ${{ vars.API_GATEWAY_URL }}
      NEXT_PUBLIC_DEFAULT_MAP_CENTER: ${{ vars.DEFAULT_MAP_CENTER || '28.6139,77.2090' }}
      NEXT_PUBLIC_USE_EMULATORS: "false"
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        run: |
          if [ -f package-lock.json ]; then npm ci; else npm install; fi

      - name: ESLint
        run: npm run lint

      - name: Type check
        run: npm run typecheck

      - name: Build static export
        run: npm run build

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: frontend-out
          path: frontend/out
          retention-days: 3

  # A checkout without cloud configuration is normal, so ask before deploying and
  # skip rather than fail. The build job above still runs everywhere and is the
  # real signal on pull requests.
  deployment-preflight:
    name: Deployment preflight
    runs-on: ubuntu-latest
    outputs:
      frontend: ${{ steps.check.outputs.frontend_configured }}
    steps:
      - uses: actions/checkout@v4

      - name: Report whether the Firebase deployment is configured
        id: check
        env:
          GCP_PROJECT_ID: ${{ vars.GCP_PROJECT_ID }}
          GCP_WORKLOAD_IDENTITY_PROVIDER: ${{ vars.GCP_WORKLOAD_IDENTITY_PROVIDER }}
          GCP_DEPLOYER_SERVICE_ACCOUNT: ${{ vars.GCP_DEPLOYER_SERVICE_ACCOUNT }}
        run: bash scripts/deployment_preflight.sh frontend

  deploy:
    name: Deploy to Firebase Hosting
    needs: [deployment-preflight, build]
    if: |
      needs.deployment-preflight.outputs.frontend == 'true' && (
        (github.event_name == 'push' && github.ref == 'refs/heads/main') ||
        github.event_name == 'workflow_dispatch'
      )
    runs-on: ubuntu-latest
    environment: ${{ vars.ENVIRONMENT || 'dev' }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Download build artifact
        uses: actions/download-artifact@v4
        with:
          name: frontend-out
          path: frontend/out

      - name: Validate deployment configuration
        env:
          GCP_PROJECT_ID: ${{ vars.GCP_PROJECT_ID }}
          GCP_WORKLOAD_IDENTITY_PROVIDER: ${{ vars.GCP_WORKLOAD_IDENTITY_PROVIDER }}
          GCP_DEPLOYER_SERVICE_ACCOUNT: ${{ vars.GCP_DEPLOYER_SERVICE_ACCOUNT }}
        run: bash "$GITHUB_WORKSPACE/scripts/check_deploy_config.sh" frontend

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ vars.GCP_WORKLOAD_IDENTITY_PROVIDER }}
          service_account: ${{ vars.GCP_DEPLOYER_SERVICE_ACCOUNT }}
          create_credentials_file: true
          export_environment_variables: true

      - name: Install Firebase CLI
        run: npm install --global firebase-tools@13

      - name: Deploy Firestore security rules
        run: |
          firebase deploy \
            --only firestore:rules \
            --project "${{ vars.GCP_PROJECT_ID }}" \
            --non-interactive \
            --force

      - name: Deploy hosting
        run: |
          firebase deploy \
            --only hosting \
            --project "${{ vars.GCP_PROJECT_ID }}" \
            --non-interactive \
            --message "Deploy ${GITHUB_SHA::7} by ${GITHUB_ACTOR}"

      - name: Summary
        run: |
          {
            echo "## VayuSetu dashboard deployed"
            echo ""
            echo "Project: ${{ vars.GCP_PROJECT_ID }}"
            echo "Commit: ${GITHUB_SHA::7}"
          } >> "$GITHUB_STEP_SUMMARY"
